// Sum of series 1 + 1/2 + 1/3...
#include <stdio.h>

int main() {
    int n; float sum = 0; printf("Enter N: "); scanf("%d", &n);
    for (int i = 1; i <= n; i++) sum += (1.0 / i);
    printf("Sum = %.2f", sum);
    return 0;
}