#include <stdio.h>
int main()
{
    int n, i;
    long long sum = 0;
    printf("Enter N: ");
    scanf("%d", &n);
    for(i = 1; i <= n; i++)
    {
        sum = sum + (i * i);
    }
    printf("Sum of series 1^2 + 2^2 + ... + %d^2 = %lld", n, sum);
    return 0;
}
