#include <stdio.h>
int main()
{
    int n, i;
    long long sum = 0;

    printf("Enter N: ");
    scanf("%d", &n);

    for(i = 1; i <= n; i++)
        sum = sum + (long long)(i * i * i);

    printf("Sum of 1^3 + 2^3 + ... + %d^3 = %lld", n, sum);

    return 0;
}
