#include <stdio.h>
int main()
{
    int n, i;
    printf("Enter N: ");
    scanf("%d", &n);
    printf("Numbers in reverse order:\n");
    for(i = n; i >= 1; i--)
    {
        printf("%d ", i);
    }
    return 0;
}
